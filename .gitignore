node_modules/
dist/
.next/
out/
*.log
.env
.env.local
.DS_Store
.tectonic-cache/
.sailor-work/

# Fetched by `bun run scripts/install-tectonic.ts` — a ~48MB binary, not source.
bin/
tsconfig.tsbuildinfo
*.tsbuildinfo
coverage/

# Regenerated by every `next dev` and `next build`, and they disagree about which
# route-types path to point at — so a tracked copy is permanently dirty.
next-env.d.ts
