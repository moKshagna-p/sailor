{
  "name": "@sailor/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "bun --env-file=../../.env drizzle-kit generate",
    "migrate": "bun --env-file=../../.env src/migrate.ts",
    "studio": "bun --env-file=../../.env drizzle-kit studio"
  },
  "dependencies": {
    "@sailor/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
